# agent-vision-toolkit 落地页

该项目使用 React + Vite 构建的落地页。视觉系统沿用了仓库现有主视觉和焦点提示说明图形中的蓝白配色方案及图像风格。

## 开发

```bash
npm install
npm run dev
```

## 验证

```bash
npm run lint
npm run build
```

生产构建产物将输出至 `dist/` 目录。

## 主要依赖

- `lucide-react`：用于界面图标
- `chart.js`：用于交互式工具路由图表
- `gsap`：用于入场、滚动及环境动效