# 安全政策

## 支持的版本

在发布带标签的版本之前，安全修复仅针对当前的 `main` 分支进行。用户在报告问题前，应针对最新修订版复现疑似问题。

## 报告漏洞

请勿针对疑似漏洞创建公开 Issue。

请通过邮件私下报告，发送至 `davidyang042@gmail.com`，邮件主题请注明 `agent-vision-toolkit security report`。如果仓库启用了 GitHub 私有漏洞报告功能，您也可以使用该渠道。

在相关情况下，请包含：

- 受影响的组件及修订版本；
- Agent 主机、操作系统及运行时版本；
- 安全影响及所需前置条件；
- 最小化复现步骤或受控的概念验证；
- 已知的建议缓解措施。

切勿包含真实的 API 密钥、Authorization 请求头、私有图片、完整的请求体或无关的对话内容。请使用占位符以及经过脱敏处理的最小化演示产物。

维护者将尽快确认收到报告，进行私下调查，并与报告者协调修复及披露事宜。

## 安全相关领域

以下类型的问题报告尤其有价值：

- 认证请求头的转发或泄露；
- 意外记录或持久化请求体、图片、提示词、对话或凭据；
- 本地代理暴露超出预期的回环接口范围；
- 请求方言混淆，导致图片数据被发送到错误的上游服务；
- CLI 工具或原生扩展中不安全的本地文件处理；
- 依赖或扩展行为超出文档声明的信任边界。

## 数据处理预期

代理应仅将图片发送至已配置的视觉 API，在转发至纯文本上游之前将其替换为文本描述，并将其描述缓存保存在内存中。日志必须仅包含操作元数据，且绝不能包含请求体、图片、提示词、对话或 API 密钥。